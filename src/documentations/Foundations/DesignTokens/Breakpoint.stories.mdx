import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@storybook-components';
import { tokens } from '@/constants';

<Meta
    title="Foundations/Design Tokens/Breakpoint"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Breakpoint"
    description="Breakpoint token scales"
>
    <DesignTokenTable tokens={tokens['breakpoint']} />
</Layout>
