import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@storybook-components';
import { tokens } from '@/constants';

<Meta
    title="Foundations/Design Tokens/Typography/LineHeight"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Line weight token scales"
>
    <DesignTokenTable tokens={tokens['line-height']} />
</Layout>
