import { Meta } from '@storybook/addon-docs';
import { Layout, Table } from '@storybook-components';
import { Text } from '@/components';
import { tokens } from '@/constants';

<Meta
    title="Foundations/Design Tokens/Typography/FontWeight"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Font weight token scales"
>
    <Table
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'value',
                label: 'Value',
            },
        ]}
        rows={Object.entries(tokens['font-weight']).map(([tokenName, token]) => ({
            name: (
                <Text weight="medium">{tokenName}</Text>
            ),
            value: token.value,
        }))}
    />
</Layout>
